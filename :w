import {Box, Tab, Tabs} from '@mui/material';
import {Link} from 'react-router-dom';



function LeftIsland() {    
    return (
        <Tabs aria-label="basic tabs example">
            <Tab component={Link} label="Projekty" to='/'/>
            <Tab component={Link} label="Propozycje" to='/propositions'/>
            <Tab component={Link} label="Ogłoszenia" to='/news'/>
        </Tabs>
    )
}

export default function Menubar() {
    return (
        <Box
          sx={{
            width: '100vw',
            display: 'flex',
            justifyContent: 'space-between',
            alignItems: 'center',
          }}
        >
            <LeftIsland />
        </Box>
    )
}
